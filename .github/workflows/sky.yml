name: Living scene

on:
  schedule:
    - cron: "23 */3 * * *"
  workflow_dispatch:
    inputs:
      weather:
        description: "Pin the weather — clear/clouds/rain/snow/fog/storm. Blank uses the real forecast."
        required: false
        default: ""
      season:
        description: "Pin the season — spring/summer/autumn/winter. Blank uses today's date."
        required: false
        default: ""
      force:
        description: "Re-render even if the scene has not changed"
        required: false
        default: "false"

permissions:
  contents: write

jobs:
  scene:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Read scene settings
        id: scene
        shell: bash
        run: python scripts/read_scene.py

      - uses: yuki4266/living-scene@v1
        with:
          lat: ${{ steps.scene.outputs.latitude }}
          lon: ${{ steps.scene.outputs.longitude }}
          tz: ${{ steps.scene.outputs.timezone }}
          weather: ${{ inputs.weather || steps.scene.outputs.weather }}
          season: ${{ inputs.season || steps.scene.outputs.season }}
          force: ${{ inputs.force }}
          commit-message: "🌦 scene sync: {state}"
